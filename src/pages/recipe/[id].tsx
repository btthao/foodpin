import React from "react";
import { Layout } from "../../components";

interface RecipePageProps {}

const RecipePage: React.FC<RecipePageProps> = ({}) => {
  return <Layout>recipe here</Layout>;
};

export default RecipePage;
